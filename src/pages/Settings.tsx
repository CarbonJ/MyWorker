// Screen 7: Settings
// - Manage Priority and Product Area dropdown option lists
// - Storage: current DB file location, change folder button
// - Export: download full DB as JSON
// - Import: upload JSON to restore (with confirmation)

export default function Settings() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-4">Settings</h1>
      <p className="text-muted-foreground">Dropdown management, storage, import/export — coming soon.</p>
    </div>
  )
}
